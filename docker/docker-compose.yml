version: '3.8'
services:
  cgs_cache:
    image: redis
    restart: always
    ports:
    - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning
    volumes: 
    - cgs_cache:/data
    
volumes:
  cgs_cache:
  driver: local